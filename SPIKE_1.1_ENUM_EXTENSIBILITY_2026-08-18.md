# Spike 1.1 — האם ניתן להוסיף ערכי enum בלי לגעת בקוד

**תאריך:** 18.8.2026 · **ענף:** `feature/parameter-management`
**שאלה:** האם `c_Enumeration` יכולה לשמש כטבלת פרמטרים שהלקוח עורך בעצמו,
ובפרט — האם הוא יכול להוסיף צורת משלוח חדשה בלי מתכנת?

**תשובה קצרה:** לעריכת תוויות — **כן, כבר היום**. להוספת ערך חדש — **לא**, ובמצב
הנוכחי הוספה כזו גם **הורסת נתונים בשקט**. יש לכך תיקון ממוקד. ראה §4.

---

## 1. איך המנגנון עובד בפועל

שלושה רכיבים, ולכל אחד תפקיד שונה ממה שהנחנו:

| רכיב | תפקיד אמיתי |
|---|---|
| `enmDeliveryMethod` (VB) | **מקור רשימת הערכים.** 15 חברים |
| `c_Enumeration` | **מקור התוויות בלבד.** 14 שורות `DeliveryMethod`, כל אחת `EnumValue` → `locText` |
| `ccEnumsController` | מאחד: `Enum.GetValues(enumType)` לרשימה, ו-`c_Enumeration` לתוויות |

**הראיה** — `WebAPI/Controllers/ccEnumsController.cs:45`:

```csharp
var values = Enum.GetValues(enumType)
```

הרשימה נבנית מה-enum **המהודר**. שורה חדשה ב-`c_Enumeration` לבדה **לא תופיע**
בשום dropdown, כי אף אחד לא קורא ממנה את רשימת הערכים.

## 2. איך הערך נשמר ונקרא

עמודות ה-enum ב-DB הן `NVARCHAR` ומחזיקות את **שם החבר**, לא מספר:

```
OrderHeader.enmDeliveryMethod = 'Biobee'
```

**זו בשורה טובה** — מספור אלפביתי של TargCC אינו מסכן נתונים קיימים, כי
הנתונים כלל אינם ממוספרים. מלכודת המספור חיה רק ב-JSON של `/api/enums`,
וכל ה-UI כבר מתאים לפי `name`.

**קריאה** — `ccOrderHeader.vb:1727, 1984`:

```vb
_DeliveryMethod = clsEnums.TranslateEnmDeliveryMethod(pReader.ReadString)
```

**כתיבה** — `ccOrderHeader.vb:1068`:

```vb
pDALParameters.Add(...).Value = _DeliveryMethod.FastToString()
```

## 3. ⚠️ הממצא המרכזי — אובדן נתונים שקט

`TranslateEnmDeliveryMethod` מסתיימת ב-`Case Else → UD`
(`csSptEnums.vb`, סוף הפונקציה). בשילוב עם הכתיבה שלמעלה נוצרת השרשרת:

```
DB: 'Elkana'  →  קריאה: Case Else → UD  →  כל שמירה כותבת: 'UD'
```

**כל ערך שאינו ב-enum נמחק בשקט ברגע שמישהו שומר את ההזמנה.** בלי שגיאה, בלי
אזהרה, בלי רישום. השדה `_DeliveryMethod` מוגדר כ-enum (`ccOrderHeader.vb:174`)
ואין שדה גולמי ששומר את המחרוזת המקורית.

**זהו בדיוק דפוס הבאג הרדום של הפרויקט:** עובר הידור, עובר smoke test,
ומתפוצץ רק כשמגיעים נתונים אמיתיים — במקרה הזה, ברגע שנייבא הזמנות עם `אלקנה`
או `YDM` מהגיליון החי.

### מה נבדק בפועל

| בדיקה | תוצאה |
|---|---|
| הזרקת `'Elkana'` ל-`OrderHeader.ID=10019` | ✅ נשמר ב-DB, העמודה מקבלת ערך חופשי |
| ניתוח נתיב הקריאה בקוד | ✅ `Case Else → UD` — אומת בקוד |
| ניתוח נתיב הכתיבה בקוד | ✅ `FastToString()` — אומת בקוד |
| **שמירה דרך ה-API וצפייה בדריסה** | ⛔ **לא בוצע** — דרוש טוקן, ראה §6 |

## 4. שלוש דרכים קדימה

### א. תוויות בלבד (מינימלי)
מסך שעורך `locText`. הלקוח משנה שמות תצוגה, לא מוסיף ערכים.
**עלות:** 1–2 ימים. **מגבלה:** לא עונה על "הלקוח יוסיף צורת משלוח".

### ב. VB שקוף לערכים לא מוכרים (מומלץ)
הוספת שדה גולמי לצד ה-enum: כשהקריאה מחזירה `UD` — נשמרת המחרוזת המקורית,
והכתיבה מחזירה אותה במקום `"UD"`. אז:

- ערך לא מוכר **שורד round-trip** במקום להימחק
- `ccEnumsController` יכול לבנות את הרשימה מ-`c_Enumeration` (איחוד עם ה-enum)
- הלקוח מוסיף ערך במסך → הוא מופיע בכל ה-dropdowns ונשמר נכון — **בלי הידור**

**עלות:** 3–5 ימים. נוגע ב-`ccOrderHeader.vb`, `ccDelivery.vb`,
`ccSupplierOrder.vb` ו-`ccEnumsController.cs`.
**סיכון:** נגיעה ידנית ב-VB — דורשת בדיקה חיה לכל ישות.

### ג. גמילה מלאה מה-enum
החלפת העמודה ב-FK לטבלה. הנכון תיאורטית, יקר בפועל — נוגע בכל פרוצדורה
ובכל קריאה, ומזמין את סחיפת ה-ordinal.
**עלות:** 2+ שבועות. **לא מומלץ עכשיו.**

## 5. המלצה

**ב', ובסדר הזה:**

1. **קודם רשת ביטחון** — הזרקת ערך זר, שמירה, ואימות שהוא שרד. זו הבדיקה
   שתיכשל היום ותעבור אחרי התיקון.
2. תיקון ה-VB לשקיפות, ישות אחת בכל פעם, בדיקה חיה אחרי כל אחת.
3. `ccEnumsController` מאחד `c_Enumeration` עם ה-enum.
4. מסך הפרמטרים — תוויות, סדר, הסתרה, ודגל `IsDelivery` בטבלת צד.

**למה זה חשוב מעבר לנוחות הלקוח:** בלי התיקון, ייבוא ההזמנות (1.5) יכניס
`אלקנה` ו-`YDM` ל-DB — ואז העריכה הראשונה של כל הזמנה כזו תמחק את צורת המשלוח
שלה. **1.1 אינו שיפור נוחות אלא תנאי מוקדם לייבוא.**

## 6. מה שלא הושלם

השלב האמפירי האחרון — שמירת ההזמנה דרך ה-API וצפייה ב-`Elkana` הופך ל-`UD` —
לא בוצע: נדרשת התחברות, ואיני מזין סיסמאות. הניתוח בקוד חד-משמעי, אבל
**במסמך הזה זו מסקנה ולא תוצאת בדיקה.** ההזמנה `ID=10019` נשארה עם `'Elkana'`
ב-DB המקומי וממתינה לבדיקה.
