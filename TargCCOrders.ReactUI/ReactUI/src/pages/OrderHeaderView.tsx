// OrderHeaderView.tsx
// Generated by CodeCreator
import { useState, useCallback } from 'react';
import {
  Box,
  Paper,
  Typography,
  Button,
  Skeleton,
  Chip,
  IconButton,
  Tooltip,
  Snackbar,
} from '@mui/material';
import Grid from '@mui/material/Grid2';
import PrintIcon from '@mui/icons-material/Print';
import ContentCopyIcon from '@mui/icons-material/ContentCopy';
import { PermissionGate } from '../contexts/AuthContext';
import { useNavigate, useParams, Link } from 'react-router-dom';
import { useOrderHeader } from '../hooks/useOrderHeader';
import { useEnumValues } from '../hooks/useEnumValues';
import { Tabs, Tab } from '@mui/material';
import { useQuery } from '@tanstack/react-query';
import AppDataGrid from '../components/shared/AppDataGrid';
import { type GridColDef } from '@mui/x-data-grid';
import { CustomerDebtApi } from '../api/CustomerDebtApi';
import { DeliveryApi } from '../api/DeliveryApi';
import { OrderLineApi } from '../api/OrderLineApi';
import { SupplierOrderApi } from '../api/SupplierOrderApi';
import { api } from '../api/client';

export default function OrderHeaderView() {
  const navigate = useNavigate();
  const { id } = useParams<{ id: string }>();
  const parsedId = id ? Number(id) : 0;
  const { data: entity, isLoading, isError } = useOrderHeader(parsedId);
  const enmPaymentMethodOptions = useEnumValues('PaymentMethod');
  const enmPaymentStatusOptions = useEnumValues('PaymentStatus');
  const enmDeliveryMethodOptions = useEnumValues('DeliveryMethod');
  const enmDeliveryDayOptions = useEnumValues('DeliveryDay');
  const enmOrderStatusOptions = useEnumValues('OrderStatus');

  const [copiedField, setCopiedField] = useState('');
  const copyToClipboard = useCallback((value: unknown, fieldName: string) => {
    navigator.clipboard.writeText(String(value ?? '')).then(() => setCopiedField(fieldName));
  }, []);

  const [activeTab, setActiveTab] = useState(0);
  const { data: customerDebtData } = useQuery({
    queryKey: ['customerDebts', 'by-parent', parsedId],
    queryFn: () => CustomerDebtApi.getAll(0, 100, '', '', 'asc', { orderHeaderId: parsedId }),
    enabled: Boolean(parsedId) && activeTab > 0,
  });
  const { data: deliveryData } = useQuery({
    queryKey: ['deliveries', 'by-parent', parsedId],
    queryFn: () => DeliveryApi.getAll(0, 100, '', '', 'asc', { orderHeaderId: parsedId }),
    enabled: Boolean(parsedId) && activeTab > 0,
  });
  const { data: orderLineData } = useQuery({
    queryKey: ['orderLines', 'by-parent', parsedId],
    queryFn: () => OrderLineApi.getAll(0, 100, '', '', 'asc', { orderHeaderId: parsedId }),
    enabled: Boolean(parsedId) && activeTab > 0,
  });
  const { data: supplierOrderData } = useQuery({
    queryKey: ['supplierOrders', 'by-parent', parsedId],
    queryFn: () => SupplierOrderApi.getAll(0, 100, '', '', 'asc', { orderHeaderId: parsedId }),
    enabled: Boolean(parsedId) && activeTab > 0,
  });
  const { data: auditData } = useQuery({
    queryKey: ['audit', 'OrderHeader', parsedId],
    queryFn: () => api.get(`/audit/OrderHeader/${parsedId}`).then((r) => r.data as { items: unknown[]; total: number }),
    enabled: Boolean(parsedId) && activeTab === 5,
  });

  if (isLoading) {
    return (
      <Box sx={{ p: 3, maxWidth: 800, mx: 'auto' }}>
        <Skeleton variant="text" width={250} height={40} sx={{ mb: 3 }} />
        <Paper sx={{ p: 3 }}>
          <Grid container spacing={2}>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
            <Grid size={{ xs: 12, sm: 6 }}>
              <Skeleton variant="text" width={80} height={20} />
              <Skeleton variant="text" width="100%" height={28} />
            </Grid>
          </Grid>
        </Paper>
      </Box>
    );
  }

  if (isError || !entity) {
    return (
      <Box p={4}>
        <Typography color="error">Order Header not found.</Typography>
      </Box>
    );
  }

  return (
    <Box sx={{ p: 3, maxWidth: 800, mx: 'auto' }}>
      <Box display="flex" justifyContent="space-between" alignItems="center" mb={3}>
        <Typography variant="h5">Order Header Details</Typography>
        <Box display="flex" gap={1}>
          <PermissionGate entity="orderHeader" action="write">
            <Button variant="contained" onClick={() => navigate(`/orderHeaders/${entity.id}/edit`)}>
              Edit
            </Button>
          </PermissionGate>
          <PermissionGate entity="orderHeader" action="write">
            <Button variant="outlined" startIcon={<ContentCopyIcon />} onClick={() => navigate(`/orderHeaders/new?cloneFrom=${entity.id}`)}>
              Clone
            </Button>
          </PermissionGate>
          <Button variant="outlined" onClick={() => navigate('/orderHeaders')}>
            Back to List
          </Button>
          <Button variant="outlined" startIcon={<PrintIcon />} onClick={() => window.print()} className="no-print">
            Print
          </Button>
        </Box>
      </Box>
      <Paper sx={{ p: 3 }}>
        <Grid container spacing={2}>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                ID
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy ID" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.id, 'ID')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.id}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Order Number
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Order Number" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.orderNumber, 'Order Number')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.orderNumber}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary" title="Customer">
                Customer ID
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Customer ID" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.fkCustomerId, 'Customer ID')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              <Link to={`/customers/${entity.fkCustomerId}`} style={{ color: 'inherit', textDecoration: 'underline' }}>
                {entity.fkCustomerId}
              </Link>
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Order Date
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Order Date" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.orderDate, 'Order Date')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {(() => { const v = entity.orderDate; if (!v || String(v) < '1900') return '-'; return new Date(v).toLocaleDateString(); })()}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Total Amount
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Total Amount" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.totalAmount, 'Total Amount')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.totalAmount ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary" title="VAT Amount">
                VATAmount
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy VATAmount" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.vatAmount, 'VATAmount')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.vatAmount ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Total With VAT
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Total With VAT" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.totalWithVat, 'Total With VAT')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.totalWithVat ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Payment Method
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Payment Method" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.enmPaymentMethod, 'Payment Method')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {enmPaymentMethodOptions.find((o) => o.value === entity.enmPaymentMethod)?.label || String(entity.enmPaymentMethod)}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Payment Status
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Payment Status" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.enmPaymentStatus, 'Payment Status')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {enmPaymentStatusOptions.find((o) => o.value === entity.enmPaymentStatus)?.label || String(entity.enmPaymentStatus)}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Payment Date
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Payment Date" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.paymentDate, 'Payment Date')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {(() => { const v = entity.paymentDate; if (!v || String(v) < '1900') return '-'; return new Date(v).toLocaleDateString(); })()}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Invoice Number
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Invoice Number" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.invoiceNumber, 'Invoice Number')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.invoiceNumber ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Delivery Method
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Delivery Method" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.enmDeliveryMethod, 'Delivery Method')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {enmDeliveryMethodOptions.find((o) => o.value === entity.enmDeliveryMethod)?.label || String(entity.enmDeliveryMethod)}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Delivery Date
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Delivery Date" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.deliveryDate, 'Delivery Date')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {(() => { const v = entity.deliveryDate; if (!v || String(v) < '1900') return '-'; return new Date(v).toLocaleDateString(); })()}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Delivery Day
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Delivery Day" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.enmDeliveryDay, 'Delivery Day')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {enmDeliveryDayOptions.find((o) => o.value === entity.enmDeliveryDay)?.label || String(entity.enmDeliveryDay)}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Order Status
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Order Status" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.enmOrderStatus, 'Order Status')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {enmOrderStatusOptions.find((o) => o.value === entity.enmOrderStatus)?.label || String(entity.enmOrderStatus)}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Notes
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Notes" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.notes, 'Notes')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.notes ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary" title="Notes 2">
                Notes2
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Notes2" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.notes2, 'Notes2')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.notes2 ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Order Month
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Order Month" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.orderMonth, 'Order Month')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.orderMonth ?? '-'}
            </Typography>
          </Grid>
          <Grid size={{ xs: 12, sm: 6 }}>
            <Box display="flex" alignItems="center" gap={0.5}>
              <Typography variant="caption" color="text.secondary">
                Quarter
              </Typography>
              <Tooltip title="Copy" className="no-print">
                <IconButton size="small" aria-label="Copy Quarter" sx={{ p: 0, opacity: 0.4, '&:hover': { opacity: 1 } }} onClick={() => copyToClipboard(entity.quarter, 'Quarter')}>
                  <ContentCopyIcon sx={{ fontSize: 14 }} />
                </IconButton>
              </Tooltip>
            </Box>
            <Typography variant="body1">
              {entity.quarter ?? '-'}
            </Typography>
          </Grid>
        </Grid>
      </Paper>

      <Box sx={{ mt: 3 }}>
        <Tabs value={activeTab} onChange={(_, v) => setActiveTab(v)} sx={{ borderBottom: 1, borderColor: 'divider' }}>
          <Tab label="Details" />
          <Tab label="Customer Debts" />
          <Tab label="Deliveries" />
          <Tab label="Order Lines" />
          <Tab label="Supplier Orders" />
          <Tab label="History" />
        </Tabs>
        {activeTab === 1 && (
          <Box sx={{ pt: 2 }}>
            <AppDataGrid
              title="Customer Debts"
              rows={customerDebtData?.items || []}
              columns={[
                { field: 'id', headerName: 'ID', width: 80 },
                { field: 'fkCustomerId', headerName: 'Customer ID', flex: 1, minWidth: 120 },
                { field: 'fkOrderHeaderId', headerName: 'Order Header ID', flex: 1, minWidth: 120 },
                { field: 'debtAmount', headerName: 'Debt Amount', flex: 1, minWidth: 120 },
                { field: 'paidAmount', headerName: 'Paid Amount', flex: 1, minWidth: 120 },
                { field: 'remainingAmount', headerName: 'Remaining Amount', flex: 1, minWidth: 120 },
              ] as GridColDef[]}
              loading={false}
              getRowId={(row) => (row as any).id as string | number}
              onRowClick={(childId) => navigate(`/customerDebts/${childId}`)}
            />
          </Box>
        )}
        {activeTab === 2 && (
          <Box sx={{ pt: 2 }}>
            <AppDataGrid
              title="Deliveries"
              rows={deliveryData?.items || []}
              columns={[
                { field: 'id', headerName: 'ID', width: 80 },
                { field: 'fkOrderHeaderId', headerName: 'Order Header ID', flex: 1, minWidth: 120 },
                { field: 'deliveryAddress', headerName: 'Delivery Address', flex: 1, minWidth: 120 },
                { field: 'contactPhone', headerName: 'Contact Phone', flex: 1, minWidth: 120 },
                { field: 'contactName', headerName: 'Contact Name', flex: 1, minWidth: 120 },
                { field: 'enmDeliveryMethod', headerName: 'Delivery Method', flex: 1, minWidth: 120 },
              ] as GridColDef[]}
              loading={false}
              getRowId={(row) => (row as any).id as string | number}
              onRowClick={(childId) => navigate(`/deliveries/${childId}`)}
            />
          </Box>
        )}
        {activeTab === 3 && (
          <Box sx={{ pt: 2 }}>
            <AppDataGrid
              title="Order Lines"
              rows={orderLineData?.items || []}
              columns={[
                { field: 'id', headerName: 'ID', width: 80 },
                { field: 'fkOrderHeaderId', headerName: 'Order Header ID', flex: 1, minWidth: 120 },
                { field: 'fkProductId', headerName: 'Product ID', flex: 1, minWidth: 120 },
                { field: 'quantity', headerName: 'Quantity', flex: 1, minWidth: 120 },
                { field: 'unitPrice', headerName: 'Unit Price', flex: 1, minWidth: 120 },
                { field: 'discountPercent', headerName: 'Discount Percent', flex: 1, minWidth: 120 },
              ] as GridColDef[]}
              loading={false}
              getRowId={(row) => (row as any).id as string | number}
              onRowClick={(childId) => navigate(`/orderLines/${childId}`)}
            />
          </Box>
        )}
        {activeTab === 4 && (
          <Box sx={{ pt: 2 }}>
            <AppDataGrid
              title="Supplier Orders"
              rows={supplierOrderData?.items || []}
              columns={[
                { field: 'id', headerName: 'ID', width: 80 },
                { field: 'fkOrderHeaderId', headerName: 'Order Header ID', flex: 1, minWidth: 120 },
                { field: 'supplierEmail', headerName: 'Supplier Email', flex: 1, minWidth: 120 },
                { field: 'emailSubject', headerName: 'Email Subject', flex: 1, minWidth: 120 },
                { field: 'emailBody', headerName: 'Email Body', flex: 1, minWidth: 120 },
                { field: 'enmEmailStatus', headerName: 'Email Status', flex: 1, minWidth: 120 },
              ] as GridColDef[]}
              loading={false}
              getRowId={(row) => (row as any).id as string | number}
              onRowClick={(childId) => navigate(`/supplierOrders/${childId}`)}
            />
          </Box>
        )}

        {/* History tab — fetches from c_AuditIndexed */}
        {activeTab === 5 && (
          <Box sx={{ pt: 2 }}>
            {auditData?.items ? (
              <Box>
                {(auditData.items as { id: number; fieldName: string; oldValue: string; newValue: string; changedByUser: string; occurredAt: string; operation: string }[]).map((a) => (
                  <Paper key={a.id} variant="outlined" sx={{ p: 1.5, mb: 1, display: 'flex', gap: 2, alignItems: 'center' }}>
                    <Typography variant="caption" color="text.secondary" sx={{ minWidth: 140 }}>
                      {new Date(a.occurredAt).toLocaleString()}
                    </Typography>
                    <Typography variant="body2" sx={{ fontWeight: 600 }}>{a.changedByUser}</Typography>
                    <Typography variant="body2">
                      {a.operation === 'Added' ? 'Created record' : `Changed ${a.fieldName}`}
                      {a.oldValue && a.newValue ? ` from "${a.oldValue}" to "${a.newValue}"` : a.newValue ? `: "${a.newValue}"` : ''}
                    </Typography>
                  </Paper>
                ))}
                {auditData.items.length === 0 && <Typography color="text.secondary">No history available</Typography>}
              </Box>
            ) : <Typography>Loading history...</Typography>}
          </Box>
        )}
      </Box>
      <Snackbar
        open={Boolean(copiedField)}
        autoHideDuration={2000}
        onClose={() => setCopiedField('')}
        message={`${copiedField} copied to clipboard`}
        anchorOrigin={{ vertical: 'bottom', horizontal: 'center' }}
      />
    </Box>
  );
}

