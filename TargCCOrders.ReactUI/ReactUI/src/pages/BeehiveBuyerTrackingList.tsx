// BeehiveBuyerTrackingList.tsx
// Generated by CodeCreator
import { useState, useCallback } from 'react';
import type { GridSortModel } from '@mui/x-data-grid';
import { type GridColDef, GridActionsCellItem } from '@mui/x-data-grid';
import { Box, Alert, Button, Dialog, DialogTitle, DialogContent } from '@mui/material';
import { Visibility as ViewIcon } from '@mui/icons-material';
import { ContentCopy as ContentCopyIcon } from '@mui/icons-material';
import { OpenInNew as OpenInNewIcon } from '@mui/icons-material';
import { Edit as EditIcon } from '@mui/icons-material';
import { Delete as DeleteIcon } from '@mui/icons-material';
import { useNavigate, Link } from 'react-router-dom';
import { useBeehiveBuyerTrackings, useUpdateBeehiveBuyerTracking, useDeleteBeehiveBuyerTracking } from '../hooks/useBeehiveBuyerTracking';
import AppDataGrid from '../components/shared/AppDataGrid';
import ConfirmDialog from '../components/shared/ConfirmDialog';
import { useNotification } from '../contexts/NotificationContext';
import { PermissionGate } from '../contexts/AuthContext';
import BeehiveBuyerTrackingForm from './BeehiveBuyerTrackingForm';
import type { UpdateBeehiveBuyerTrackingRequest } from '../types/BeehiveBuyerTracking';
import FilterBuilder from '../components/shared/FilterBuilder';
import FilterListIcon from '@mui/icons-material/FilterList';
import { Collapse } from '@mui/material';

export default function BeehiveBuyerTrackingList() {
  const [paginationModel, setPaginationModel] = useState({ page: 0, pageSize: 25 });
  const [search, setSearch] = useState('');
  const [sortModel, setSortModel] = useState<GridSortModel>([]);
  const [showFilters, setShowFilters] = useState(false);
  const [activeFilters, setActiveFilters] = useState<Record<string, unknown>>({});
  const sortField = sortModel[0]?.field ?? '';
  const sortDir = sortModel[0]?.sort ?? 'asc';
  const handleSortModelChange = useCallback((model: GridSortModel) => {
    setSortModel(model);
    setPaginationModel((prev) => ({ ...prev, page: 0 }));
  }, []);
  const { data, isLoading, isError, error } = useBeehiveBuyerTrackings(paginationModel.page, paginationModel.pageSize, search, sortField, sortDir);
  const deleteMutation = useDeleteBeehiveBuyerTracking();
  const [deleteId, setDeleteId] = useState<number | null>(null);
  const [selectedIds, setSelectedIds] = useState<(string | number)[]>([]);
  const [bulkDeleteConfirm, setBulkDeleteConfirm] = useState(false);
  const navigate = useNavigate();
  const { showSuccess } = useNotification();
  const updateMutation = useUpdateBeehiveBuyerTracking();
  const [quickCreateOpen, setQuickCreateOpen] = useState(false);

  const columns: GridColDef[] = [
    { field: 'id', headerName: 'ID', width: 80, type: 'number' },
    {
      field: 'fkCustomerId',
      headerName: 'Customer ID',
      width: 120,
      renderCell: (params) => params.value ? (
        <Link to={`/customers/${params.value}`} style={{ color: 'inherit', textDecoration: 'underline' }}  onClick={(e) => e.stopPropagation()}>
          {params.value}
        </Link>
      ) : '',
    },
    {
      field: 'lastOrderDate',
      headerName: 'Last Order Date',
      width: 130,
      valueFormatter: (value) => value ? new Date(value as string).toLocaleDateString() : '',
    },
    { field: 'beehiveQuantity', headerName: 'Beehive Quantity', width: 120 },
    { field: 'reminderMonth', headerName: 'Reminder Month', width: 120 },
    { field: 'isRelevant', headerName: 'Is Relevant', width: 100, type: 'boolean' },
    { field: 'notes', headerName: 'Notes', flex: 1, minWidth: 150 },
    {
      field: 'actions',
      type: 'actions',
      headerName: 'Actions',
      width: 140,
      getActions: (params) => [
        <GridActionsCellItem
          key="view"
          icon={<ViewIcon />}
          label="View"
          onClick={() => navigate(`/beehiveBuyerTrackings/${params.id}`)}
        />,
        <GridActionsCellItem
          key="edit"
          icon={<EditIcon />}
          label="Edit"
          onClick={() => navigate(`/beehiveBuyerTrackings/${params.id}/edit`)}
        />,
        <GridActionsCellItem
          key="delete"
          icon={<DeleteIcon />}
          label="Delete"
          onClick={() => setDeleteId(params.id as number)}
        />,
      ],
    },
  ];

  if (isError) {
    return (
      <Box sx={{ p: 3 }}>
        <Alert severity="error" sx={{ mb: 2 }}>
          Failed to load beehive buyer trackings: {(error as Error)?.message || 'Unknown error'}
        </Alert>
      </Box>
    );
  }

  const filterFields = [
    { field: 'fkCustomerId', label: 'Customer ID', type: 'number' as const },
    { field: 'lastOrderDate', label: 'Last Order Date', type: 'date' as const },
    { field: 'beehiveQuantity', label: 'Beehive Quantity', type: 'number' as const },
    { field: 'reminderMonth', label: 'Reminder Month', type: 'number' as const },
    { field: 'isRelevant', label: 'Is Relevant', type: 'boolean' as const },
    { field: 'notes', label: 'Notes', type: 'string' as const },
  ];

  return (
    <Box sx={{ p: 3 }}>
      <Box display="flex" justifyContent="flex-end" mb={1}>
        <Button size="small" startIcon={<FilterListIcon />} onClick={() => setShowFilters((p) => !p)} color={Object.keys(activeFilters).length > 0 ? 'primary' : 'inherit'}>
          {showFilters ? 'Hide Filters' : `Filters${Object.keys(activeFilters).length > 0 ? ` (${Object.keys(activeFilters).length})` : ''}`}
        </Button>
      </Box>
      <Collapse in={showFilters}>
        <FilterBuilder fields={filterFields} onApply={(f) => { setActiveFilters(f as Record<string, unknown>); setPaginationModel((p) => ({ ...p, page: 0 })); }} onClear={() => setActiveFilters({})} />
      </Collapse>
      <AppDataGrid
        entityName="beehiveBuyerTracking"
        title="Beehive Buyer Tracking List"
        rows={data?.items || []}
        columns={columns}
        loading={isLoading}
        getRowId={(row) => (row as any).id as string | number}
        rowCount={data?.total || 0}
        paginationModel={paginationModel}
        onPaginationModelChange={setPaginationModel}
        serverSearch={search}
        onServerSearchChange={setSearch}
        sortModel={sortModel}
        onSortModelChange={handleSortModelChange}
        onRowClick={(id) => navigate(`/beehiveBuyerTrackings/${id}`)}
        checkboxSelection
        onSelectionChange={(ids) => setSelectedIds(ids)}
        onContextMenuActions={(id, row) => [
          { label: 'Open', icon: <OpenInNewIcon fontSize="small" />, onClick: () => navigate(`/beehiveBuyerTrackings/${id}`) },
          { label: 'Copy ID', icon: <ContentCopyIcon fontSize="small" />, onClick: () => { navigator.clipboard.writeText(String(id)); showSuccess('ID copied'); } },
          { label: 'Copy Row', icon: <ContentCopyIcon fontSize="small" />, onClick: () => { navigator.clipboard.writeText(JSON.stringify(row, null, 2)); showSuccess('Row copied'); } },
          { label: 'Duplicate', icon: <ContentCopyIcon fontSize="small" />, onClick: () => navigate(`/beehiveBuyerTrackings/new?cloneFrom=${id}`), divider: true },
          { label: 'Edit', icon: <EditIcon fontSize="small" />, onClick: () => navigate(`/beehiveBuyerTrackings/${id}/edit`) },
          { label: 'Delete', icon: <DeleteIcon fontSize="small" />, onClick: () => setDeleteId(id as number), divider: true },
        ]}
        onCreateNew={() => navigate('/beehiveBuyerTrackings/new')}
        editable
        processRowUpdate={async (newRow, oldRow) => { await updateMutation.mutateAsync({ id: (newRow as any).id as number, data: newRow as unknown as UpdateBeehiveBuyerTrackingRequest }); return newRow; }}
        onBulkEdit={async (ids, field, value) => {
          await Promise.all(ids.map((id) => updateMutation.mutateAsync({ id: id as number, data: { [field]: value } as unknown as UpdateBeehiveBuyerTrackingRequest })));
        }}
        bulkEditableFields={[
          { field: 'fkCustomerId', label: 'Customer ID', type: 'number' },
          { field: 'lastOrderDate', label: 'Last Order Date', type: 'date' },
          { field: 'beehiveQuantity', label: 'Beehive Quantity', type: 'number' },
          { field: 'reminderMonth', label: 'Reminder Month', type: 'number' },
          { field: 'notes', label: 'Notes', type: 'string' },
        ]}
        headerActions={
          <>
            <PermissionGate entity="beehiveBuyerTracking" action="delete">
              {selectedIds.length > 0 && (
                <Button variant="outlined" color="error" onClick={() => setBulkDeleteConfirm(true)}>
                  Delete Selected ({selectedIds.length})
                </Button>
              )}
            </PermissionGate>
            <PermissionGate entity="beehiveBuyerTracking" action="write">
              <Button variant="contained" onClick={() => navigate('/beehiveBuyerTrackings/new')}>
                Add New
              </Button>
              <Button variant="outlined" onClick={() => setQuickCreateOpen(true)}>
                Quick Create
              </Button>
            </PermissionGate>
          </>
        }
      />
      <Dialog open={quickCreateOpen} onClose={() => setQuickCreateOpen(false)} maxWidth="md" fullWidth>
        <DialogTitle>Quick Create Beehive Buyer Tracking</DialogTitle>
        <DialogContent>
          <BeehiveBuyerTrackingForm embedded onSuccess={() => setQuickCreateOpen(false)} />
        </DialogContent>
      </Dialog>
      <ConfirmDialog
        open={deleteId !== null}
        title="Delete Beehive Buyer Tracking"
        message="Are you sure you want to delete this beehive buyer tracking? This action cannot be undone."
        confirmDisabled={deleteMutation.isPending}
        onConfirm={() => {
          if (deleteId !== null) {
            deleteMutation.mutate(deleteId);
          }
          setDeleteId(null);
        }}
        onCancel={() => setDeleteId(null)}
      />
      <ConfirmDialog
        open={bulkDeleteConfirm}
        title="Delete Multiple Beehive Buyer Trackings"
        message={`Are you sure you want to delete ${selectedIds.length} beehive buyer tracking(s)? This action cannot be undone.`}
        severity="error"
        onConfirm={async () => {
          try {
            await Promise.all(selectedIds.map((id) => deleteMutation.mutateAsync(id as number)));
          } catch { /* individual errors handled by mutation onError */ }
          setSelectedIds([]);
          setBulkDeleteConfirm(false);
        }}
        onCancel={() => setBulkDeleteConfirm(false)}
      />
    </Box>
  );
}

