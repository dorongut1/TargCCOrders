# דוח מוכנות לעלייה + מדריך פריסה — TargCCOrders
**תאריך:** 17.7.2026 | מבוסס על בדיקה בפועל של הדיסק, הלוגים, ה-git וה-build — לא הערכות.

---

## 1. מה בדקתי עכשיו ומה המצב

**קוד ✅** — כל התיקונים במקום ואומתו על הדיסק: הגנות ה-StackFrame (clsRequester, clsFault, ccSecurity, ccHelper), בלם הרקורסיה, RequesterFactory בכל 16 הקונטרולרים, העברית ב-React, ותיקון ה-@import. אפס שינויים לא-שמורים ב-git.

**לוגים ✅** — בלוג של היום שגיאה אחת בלבד (08:13 — לפני התיקון). מאז: עליות נקיות של השרת, אפס שגיאות. זו עדות אמפירית שהתיקונים עובדים.

**Build ✅** — בניית פרודקשן של ה-React (`npm run build`) עוברת נקי (בדקתי אצלי). אזהרת chunk גדול (ExcelJS) — קוסמטית, לשיפור בהמשך.

**git ⚠️** — ה-commit העובד `3fe9b24` קיים מקומית אבל **טרם נדחף** (ahead 1 מול origin). ה-push חייב לרוץ אצלך.

**DB ✅ (כמעט)** — תיקון הפרוצדורות (`@WithParentText`) רץ והצליח. נשאר לאמת ידנית שעמוד ההזמנות נטען (דקה אחת).

---

## 2. חסמים לפני שהמנכ"ל נוגע — לפי סדר (⚠️ = פעולה שלך)

**⚠️ א. מפתח JWT — עדיין placeholder!** `appsettings.json` עדיין מכיל `CHANGE-ME...`. כל מי שמכיר את הקוד יכול לזייף כניסת אדמין. שורה אחת: הרץ ב-PowerShell
```powershell
[Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }))
```
ושים את התוצאה ב-`Jwt:AdminKey`. (אחרי החלפה — התחברות מחדש לכולם.)

**⚠️ ב. סיסמת Admin.** אם היא עדיין `password` — להחליף לחזקה לפני שאנשים נוספים רואים את המערכת.

**⚠️ ג. git push.** פקודה אחת בטרמינל: `git push`. בלי זה אין גיבוי מחוץ למחשב.

**⚠️ ד. בדיקת עשן (5 דקות) — החשוב ביותר:** התחברות → עמוד הזמנות נטען (מאמת את תיקון ה-SP) → יצירת הזמנה חדשה עם 2 שורות → סכום כולל מע"מ לא-אפס → מחיקת שורה ושמירה → דשבורד מציג מספרים. אם הכל ירוק — המערכת מוכנה טכנית.

**⚠️ ה. נתונים! זה מה שיקבע את הרושם של המנכ"ל.** המערכת כרגע ריקה כמעט לגמרי. מנכ"ל ש"משחק" במערכת ריקה רואה שלד; מנכ"ל שרואה את הלקוחות והמוצרים האמיתיים שלו — רואה מוצר. שתי אפשרויות: (1) המהלך הנכון — ייבוא ההיסטוריה מהאקסל (לקוחות, מוצרים, מחירון, הזמנות). אני אבנה את סקריפט הייבוא — רק תגיד. (2) מינימום — הזנה ידנית של 10–15 לקוחות ומוצרים אמיתיים + כמה הזמנות.

**ניקיון (רשות):** תיקיית `_to_delete` (108MB) — למחוק; `ccSecurity.vb.bak` — למחוק.

---

## 3. איך מעלים לשרת שלכם — שתי דרכים

### דרך מהירה (דמו פנימי מיידי, בלי שרת): להריץ מהמחשב שלך ברשת המשרד
מתאים אם המנכ"ל באותה רשת ורוצים לשחק כבר היום:
1. ב-VS: **Publish** של TargCCOrders.WebAPIHost ל-Folder (`Release`).
2. העתק את תוכן `TargCCOrders.ReactUI\ReactUI\dist` לתיקיית `wwwroot` בתוך תיקיית ה-Publish.
3. בתיקיית ה-Publish ערוך `TargCCOrders.WebAPIHost.dll.config` — ודא `TargCCOrders.Controller` נכון, ו-`appsettings.json` עם מפתח JWT אמיתי.
4. הרץ: `TargCCOrders.WebAPIHost.exe --urls http://0.0.0.0:8080` ופתח את פורט 8080 ב-Windows Firewall.
5. המנכ"ל גולש ל-`http://<שם-המחשב-שלך>:8080` — מקבל את האפליקציה המלאה (ה-API מגיש גם את ה-React).
⚠️ בלי HTTPS — לדמו פנימי בלבד, לא לשימוש שוטף.

### דרך נכונה (פרודקשן): Windows Server + IIS
1. **דרישות שרת:** Windows Server, התקנת **.NET 8 Hosting Bundle** (חיפוש: "dotnet hosting bundle 8"), גישה ל-SQL Server.
2. **DB:** גיבוי `TargCCOrdersNew` מהמחשב שלך (BACKUP DATABASE) → RESTORE על ה-SQL של השרת. זה מביא הכל — טבלאות, פרוצדורות (כולל התיקון), הגדרות ונתונים. אם מופיעה שגיאת "principal dbo" אחרי restore: `ALTER AUTHORIZATION ON DATABASE::TargCCOrdersNew TO sa;`
3. **משתמש SQL לאפליקציה:** צור לוגין SQL (למשל `TargCCApp`), ואז על ה-DB: `GRANT EXECUTE TO TargCCApp;` (TargCC עובד דרך פרוצדורות — EXECUTE מספיק, זה גם יותר מאובטח).
4. **Publish:** כמו בדרך המהירה (Publish + dist→wwwroot), והעתק את התיקייה לשרת.
5. **קונפיגורציה בשרת:**
   - `TargCCOrders.WebAPIHost.dll.config`: `TargCCOrders.Controller = <SQLSERVER>~TargCCOrdersNew~500~TargCCApp~<סיסמה>` וכן `LogLocation` לתיקייה קיימת וכתיבה (למשל `D:\Logs\TargCC`).
   - `appsettings.json`: מפתח JWT אמיתי (שונה מזה של הפיתוח!), `Cors:AllowedOrigins` עם הכתובת האמיתית של האתר.
   - משתנה סביבה `ASPNETCORE_ENVIRONMENT=Production` (מכבה Swagger, מדליק HSTS).
6. **IIS:** צור Application Pool חדש (No Managed Code) → צור Site שמצביע לתיקיית ה-Publish → Binding ל-HTTPS עם תעודה (יש לכם תעודת דומיין? אם לא — תעודה פנימית או Let's Encrypt דרך win-acme).
7. **תזמון יומי** (SQL Agent Job בשרת): `EXEC sp_UpdateDebtAttention; EXEC sp_RefreshBeehiveTracking;`
8. **בדיקת עשן בשרת:** `https://<כתובת>/api/health` → `{"status":"ok","db":"ok"}` → התחברות → הזמנות.

---

## 4. תזכורות קבועות

- **לא מריצים CC** על הפתרון — ראינו מה קורה. אם אי-פעם חייבים (שינוי DB), עושים את זה יחד ובאופן מבוקר.
- כל שינוי מהיום והלאה: commit + push מיד. ה-commit `3fe9b24` הוא נקודת השחזור המאומתת.
- הסקריפטים בתיקיית `Database\` הם חלק מהפריסה — אם מרימים DB נקי בשרת (ולא restore), מריצים אותם לפי הסדר.

## 5. אחרי העלייה — הצעד הבא בעל הערך הגבוה ביותר

ייבוא נתוני האקסל ההיסטוריים (לקוחות → מוצרים → מחירון → הזמנות → חובות). זה מה שהופך את המערכת מ"אתר יפה" ל"החלפת האקסל" בעיני המנכ"ל. אני מוכן לבנות את סקריפט הייבוא ברגע שתגיד.
