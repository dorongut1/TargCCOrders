# עיצוב — עברות המסכים המיוצרים והחלפת מזהי FK בשמות

**תאריך:** 17.8.2026 · **סטטוס:** עיצוב מאושר, טרם מומש
**הקשר:** המערכת עלתה לשרת טסטים שיוצג למנכ"ל. המסכים המיוצרים ע"י TargCC
מוצגים באנגלית ומראים `Customer ID: 3` במקום שם הלקוח.

---

## 1. מה קובע את העיצוב

**TargCC לא ירוץ שוב.** הוחלט שכל שינוי סכמה עתידי ייכתב ידנית. לכן הקבצים
המיוצרים הם מעכשיו קוד רגיל, ומותר לערוך אותם ישירות. זה מייתר את האפשרות
של תיקון בתבניות הגנרטור.

**הרף הוא סיור של מנכ"ל.** כל מה שנגיש מהתפריט חייב להיראות גמור, כי אי אפשר
לנבא על מה ילחץ. המסלול הקריטי: תפריט ← רשימה ← לחיצה על שורה ← מסך צפייה.

**מדידות שנלקחו מהקוד ומה-DB (17.8.2026):**

| מדד | ערך |
|---|---|
| עמודים שאינם משתמשים ב-`useTranslation` | 36 מתוך 43 |
| שדות ייחודיים המוצגים בעמודות | 86 |
| מהם כבר במילון `he.ts` | ~45 |
| יעדי FK בכל המערכת | **3 בלבד** |
| לקוחות · מוצרים · מחירון · הזמנות ב-DB | 1,312 · 100 · 199 · 17 |

---

## 2. היקף

**בפנים:** 7 ישויות גלויות בתפריט × שלושה מסכים (List / View / Form):
`customer`, `product`, `productPrice`, `productPriceHist`, `delivery`,
`supplierOrder`, `beehiveBuyerTracking`.
בנוסף `orderHeader` ו-`orderLine` המוסתרים — זולים ברגע שהמילון קיים, ונגישים
ב-deep link.

**בחוץ:** paging בצד השרת · ייצוא לריווחית · תזכורות כוורות · ייבוא ההזמנות
ההיסטוריות. כל אלה סעיפים נפרדים במסמך ההעברה.

---

## 3. ארכיטקטורה

שלושה חלקים בלתי תלויים. כל אחד ניתן לבדיקה בנפרד.

### 3.1 `useEntityLookup` — פתרון מזהי ה-FK

קובץ חדש: `src/hooks/useEntityLookup.ts`

קבוצת היעדים סגורה וידועה — שלושה בלבד:

| שדה | ישות יעד | מחרוזת תצוגה | נפילה |
|---|---|---|---|
| `fkCustomerId` | Customer | `customerName` | `לקוח #3` |
| `fkProductId` | Product | `productName` | `מוצר #7` |
| `fkOrderHeaderId` | OrderHeader | `הזמנה #1042` (לפי `orderNumber`) | `הזמנה #12` |

**חוזה:** ההוק מקבל שם ישות ומחזיר `(id: number) => string`, מגובה ב-`Map`
שנבנה פעם אחת ב-`useMemo`. לא `Array.find` בכל תא — התקדים הקיים ב-
`DebtManagementList.tsx:145` הוא O(n²), וכאן זה 1,312 × 25 בכל רינדור.

**מקור הנתונים:** אותם `queryKey` שכבר בשימוש — `['customers','all']`,
`['products','all']`. react-query מגיש מה-cache המשותף, כך שברוב המקרים
**לא נוצרת בקשת רשת נוספת**. זה הדפוס שכבר קיים ב-`OrderCompositeForm`.

**החלטה מכוונת — הנפילה אינה ריקה.** בזמן טעינה או כשהרשומה נמחקה, תא ריק
נראה כמו תקלה. `לקוח #3` נראה כמו נתון ומשמר את המידע שהיה שם קודם.

**הקישור לרשומה (`<Link>`) נשמר** — רק הטקסט מוחלף.

### 3.2 שכבת התוויות

**מקור אמת יחיד:** `src/i18n/he.ts`, מורחב ב-~41 השדות החסרים.

**היירוט ב-`AppDataGrid`** מכסה בבת אחת את כל מסכי הרשימה:
- `headerName` מתורגם לפי שם השדה
- `title` נגזר מ-`entityName` דרך `entities[x].p`
- ה-UI הפנימי של הרכיב (סרגל, תפריטים, מצב ריק) מקבל ברירות מחדל עבריות

**כלל היירוט — מפורש, לא היוריסטי.** עמודה מתורגמת רק אם מתקיימים **שני**
התנאים:
1. קיימת לשדה תווית עברית במילון, **וגם**
2. מה שהקורא העביר ב-`headerName` אינו כבר עברית — מבחן `/[\u0590-\u05FF]/`

כך המסכים שכבר עובדים בעברית (`DebtManagementList`, `OrderCompositeList`,
`DeliveryWorkflowList`) אינם נגעים, ומסך שרוצה תווית חורגת פשוט מעביר עברית
משלו ומנצח.

### 3.3 עריכות בקבצים

מה ש-`AppDataGrid` אינו מגיע אליו, בכל אחד מהמסכים:
הודעת השגיאה · נוסח דיאלוג המחיקה · תוויות `filterFields` ו-`bulkEditableFields` ·
`Add New` / `Quick Create` · תוויות תפריט ההקשר.
כולם שואבים מאותו מילון — אין מחרוזות עברית מפוזרות.

מסכי `View` ו-`Form` נערכים ישירות: תוויות שדות, כותרות, כפתורים והודעות ולידציה.

---

## 4. ניקוי `addFieldsHere`

מסך `היסטוריית מחירים` מציג עמודה בשם **`Add Fields Here`** — placeholder של
הגנרטור. אומת מול ה-DB: **העמודה אינה קיימת בטבלה `ProductPriceHist`**. היא
חיה רק בטיפוס TypeScript ובשלושת המסכים.

**פעולה:** להסיר את `addFieldsHere` מ-`types/ProductPriceHist.ts` ומשלושת
המסכים. לא לתרגם שדה רפאים.

---

## 5. סיכונים

### 5.1 `AppDataGrid` הוא רכיב משותף — זהו הסיכון המרכזי

הרכיב משרת **כל** מסך רשימה, כולל שלושה שעובדים היום נכון בעברית. באג שם לא
שובר מסך אחד אלא את כולם — כולל מסך הלקוחות, שהוא הבדיקה לסחיפת ordinal.

זה המחיר של הריכוזיות, והוא מודע: 150+ עריכות חוזרות היו מייצרות חוסר עקביות
גלוי. המסקנה המעשית היא שהאימות חייב לכלול את שלוש בדיקות הליבה, לא רק
"המסכים בעברית".

### 5.2 למה **לא** נוגעים בשרת

הדרך ה"נכונה" לכאורה — להוסיף עמודות שם ל-`SELECT` בפרוצדורות — **נדחתה
במכוון**. שכבת ה-VB קוראת עמודות לפי מיקום, ו-CC כבר לא יכול לייצר אותה מחדש.
זהו בדיוק דפוס סחיפת ה-ordinal שכבר פוצץ את הפרויקט שלוש פעמים
(`VATRatePercent`, `RivhitCustomerNo`, `clc_RemainingAmount`), והוא **רדום** —
עובר הידור, עובר smoke test, ומתפוצץ כשמגיעים נתונים.

הפתרון בצד הלקוח לא נוגע ב-DB ולא ב-VB. אפס סיכון ordinal עבור שיפור שהוא
במהותו תצוגתי.

**מגבלה ידועה:** ההוק טוען את הרשימה המלאה. ב-1,312 לקוחות זה זניח וזה ממילא
כבר קורה היום. בסדר גודל של 20,000 זה ייפול — אבל אז גם ה-paging ייפול, וזה
תיקון נפרד.

---

## 6. תוכנית אימות

הידור נקי אינו הוכחה. שני הבאגים הגרועים בפרויקט עברו הידור מושלם ונכשלו בזמן ריצה.

**סטטי:**
1. `npx tsc --noEmit` — נקי
2. `npm run build` — עם exit code אמיתי (`cmd /c "npm run build & echo EXIT=%ERRORLEVEL%"`)

**חי, מול `localhost\TargCCOrdersNew`:**

3. מעבר על כל 7 מסכי הרשימה: עברית מלאה **וגם** שמות שנפתרים בפועל —
   עמודת לקוח מציגה שם, לא `לקוח #3`
4. פתיחת מסך `View` ומסך `Form` לכל ישות
5. `היסטוריית מחירים` — אין עמודת `Add Fields Here`

**שלוש בדיקות הליבה — לא מדלגים:**

| בדיקה | מאמת |
|---|---|
| מסך לקוחות מציג 1,312 | סחיפת ordinal |
| הזמנה חדשה נשמרת כ**"חדש"** ולא "מבוטל" | ה-enum |
| שמירה ופתיחה מחדש — מע"מ אינו אפס | המע"מ |

**בדיקת סעיף 3 שנלוותה:** בחירת מוצר בלי מחיר (אחד מ-16) → ההתראה קופצת.

---

## 7. תקלות רדומות שאותרו ולא מטופלות כאן

| ממצא | מצב | למה נדחה |
|---|---|---|
| `ProductPriceHist.enmCustomerType` — הטיפוס אומר `number`, ה-DB אומר `nvarchar` | רדום | הטבלה ריקה (0 שורות). תיקון דורש בדיקה מול שכבת VB — סעיף נפרד. |
| עמודות `TableName_*` ב-19 טבלאות | שפיר | שארית TargCC, אינן מרונדרות בשום מסך. |
| 16 מוצרים בלי מחיר | פתוח | סעיף 5ב במסמך ההעברה — החלטה עסקית, לא קוד. |

---

## 8. מיקום העבודה

העץ הראשי `C:\Dev\NonTFS\TargCCOrders` על `main`. ה-worktree של הסשן נקי ואין
בו `node_modules`, כך שאי אפשר לבנות או לאמת בו. זה גם המקום שבו יושב מחזור
ה-publish המתועד ב-`DEPLOY_TO_SERVER.md`.
