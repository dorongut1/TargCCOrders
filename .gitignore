# ── Visual Studio / .NET ──────────────────────────────
.vs/
bin/
obj/
*.user
*.suo
*.cache
*.pdb
TestResults/
packages/
*.slnLaunch.user

# ── React / Node ──────────────────────────────────────
node_modules/
TargCCOrders.ReactUI/ReactUI/dist/
*.tsbuildinfo
npm-debug.log*

# ── Logs ──────────────────────────────────────────────
Logs/
*.log

# ── סודות — לעולם לא לגיט ────────────────────────────
# מפתח ה-JWT האמיתי צריך לשבת רק על השרת:
# appsettings.Production.json מוחרג, appsettings.json נשאר עם placeholder
appsettings.Production.json
*.secrets.json

# ── זמניים ────────────────────────────────────────────
_to_delete/
zzPreviousRole_Backup*

# ── ארטיפקטים של סשן עבודה — אסור שייכנסו לריפו ──────
# _token.txt מכיל JWT חי עם כרטיס TargCC מוצפן בתוכו
_token*.txt
_api_out*.log
_api_err*.log
_build.log
# גיבוי הקונטרולרים לפני הנעילה — git עצמו הוא הגיבוי
WebAPI/_backup_controllers_*/
Database/_lockdown_*.ps1
Database/_whoLocks.ps1
_commitmsg.txt
_reactbuild.log

# ── קבצי עזר גדולים (האקסל המקורי — גיבוי, לא קוד) ──
*.xlsx
