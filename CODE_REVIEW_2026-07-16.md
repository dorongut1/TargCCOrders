# דוח Code Review ושיפורים — TargCCOrders
**תאריך:** 16.7.2026 | **סוקר:** Claude | **היקף:** כל הפרויקט — DB, WebAPI, React, שכבת VB, מול האקסל המקורי ומסמכי TargCC

> הדוח טכני מטבעו (זו סקירת קוד), אבל כתבתי אותו כך שסדר העדיפויות והמשמעות העסקית ברורים גם בלי לצלול לקוד. כל ממצא מסומן: ✅ תוקן על ידי | ⚠️ דורש פעולה שלך | 📋 מומלץ להמשך.
>
> בדקתי גם הזרקות פרומפט בקוד, במסמכי הפרויקט ובהנחיות — לא נמצאו.

---

## תמצית מנהלים

המערכת בנויה על יסודות טובים: סכמה מסודרת עם FK ואינדקסים, TargCC מייצר שכבת נתונים עם הרשאות ו-Audit מובנים, וה-React בנוי נכון (react-query, ולידציה, RTL). **אבל היא לא הייתה מוכנה לעלייה לאוויר** — נמצאו 8 ממצאים קריטיים, שהחמורים בהם: כל בקשת API רצה כמשתמש-על שעוקף את כל מערך ההרשאות, וסכומי ההזמנות לא חושבו מעולם (כל ההזמנות היו ₪0). את רוב הקריטיים תיקנתי בקוד; נשארו לך צעדי קונפיגורציה והרצת סקריפט DB.

**מצב אחרי השיפורים:** המערכת קרובה למוכנות לדמו למנכ"ל, בכפוף ל-6 צעדים ברשימת ה"חובה לפני עלייה" בסוף הדוח.

---

## 1. ממצאים קריטיים

### C1 — עקיפה מלאה של מערך ההרשאות ✅ תוקן
כל הקונטרולרים יצרו את המשתמש כך: `new clsRequester(userName, "View", true)` — זה הבנאי של **SecurityExempt** ב-TargCC. שם המשתמש האמיתי נזרק, כל בדיקות ההרשאה דולגו, וכל רשומת Audit נרשמה על שם "SecurityExempt" במקום המשתמש האמיתי. כלומר: כל מי שמחובר יכול היה לעשות הכל, ולא היה שום תיעוד מי באמת שינה מה.
**התיקון:** בזמן Login נוצר Ticket מוצפן של TargCC (עם המשתמש, התפקידים והזהות האמיתיים) ומוטמע בתוך ה-JWT. כל בקשה משחזרת ממנו Requester אמיתי (`ccRequesterFactory.cs` חדש + עדכון כל 11 הקונטרולרים). ההרשאות וה-Audit של TargCC עובדים עכשיו כמתוכנן.

### C2 — סכומי הזמנות תמיד ₪0 ✅ תוקן (דורש הרצת סקריפט)
`clc_TotalAmount / clc_VATAmount / clc_TotalWithVAT` ב-OrderHeader הן עמודות רגילות עם ברירת מחדל 0 — אין להן שום מנגנון חישוב: לא computed, לא טריגר, לא קוד. כל המסכים, הדוחות ומעקב החובות קראו אפסים.
**התיקון:** טריגר `trg_OrderLine_RecalcHeaderTotals` שמעדכן את הכותרת בכל שינוי שורה + פרוצדורת `sp_RecalcAllOrderTotals` לחישוב רטרואקטיבי (בסקריפט ה-DB).

### C3 — מע"מ שגוי (17% קשיח) ✅ תוקן
המע"מ בישראל הוא 18% מ-1.1.2025, וה-React חישב `subtotal * 0.17` קשיח. בדמו מול חשבונית אמיתית זה היה מתפוצץ.
**התיקון:** עמודת `VATRatePercent` פר-הזמנה (snapshot — הזמנות ישנות נשארות ב-17%), הגדרות עסקיות ב-`c_SystemDefault`, endpoint חדש `/api/settings/business`, וה-React מושך משם את השיעור.

### C4 — מחיקת שורת הזמנה לא נשמרה מעולם ✅ תוקן
בטופס ההזמנה, מחיקת שורה עדכנה רק את המסך; בשמירה השורה חזרה. בנוסף, השמירה לא הייתה אטומית — כשל באמצע השאיר הזמנה חצי-שמורה, ולחיצה חוזרת על "שמור" שכפלה שורות.
**התיקון:** endpoint חדש `POST/PUT /api/orders/composite` ששומר כותרת+שורות+מחיקות בקריאה אחת, עם פיצוי (מחיקת כותרת אם אף שורה לא נשמרה), שגיאות פר-שורה, ומספור הזמנה אוטומטי. הטופס עודכן להשתמש בו.

### C5 — תמחור אוטומטי לפי סוג לקוח היה שגוי ✅ תוקן
המחירון בנוי לפי (מוצר × סוג לקוח), אבל ה-UI חיפש מחיר לפי `customerId` — פרמטר שהשרת מתעלם ממנו — וקיבל את שורת המחיר הראשונה שנמצאה. לקוח חקלאי היה יכול לקבל מחיר קמעונאי ולהפך. בנוסף, האינדקס במחירון לא היה ייחודי — שתי שורות מחיר סותרות לאותו (מוצר, סוג) היו חוקיות.
**התיקון:** endpoint חדש `/api/pricing/resolve` שפותר את המחיר בצד השרת לפי סוג הלקוח האמיתי + כמות (תמיכה במדרגות כמות), אינדקס ייחודי במחירון (עם ניקוי כפילויות בסקריפט), וה-React עודכן.

### C6 — הטריגר של עלות הקנייה הרס נתוני רווחיות ✅ תוקן (בסקריפט)
`trg_OrderLine_SetUnitCost` דרס את עלות הקנייה ההיסטורית בכל עדכון שורה — כשביובי מעלה מחירים, כל עריכה של הזמנה ישנה הייתה משכתבת את הרווחיות ההיסטורית. הוחלף בטריגר שקובע עלות רק בשורה חדשה/החלפת מוצר.

### C7 — מפתח JWT חשוף בקוד ✅ תוקן (דורש הגדרת סוד)
היה fallback קשיח `"DefaultDevKey_CHANGE_IN_PRODUCTION_!!"` — אם הקונפיגורציה חסרה, כל אחד שמכיר את הקוד יכול לזייף טוקן אדמין. הוסר; עכשיו האפליקציה נופלת בעלייה אם אין מפתח אמיתי. ⚠️ **עליך להגדיר מפתח 32+ תווים** (מומלץ במשתנה סביבה, לא בקובץ).

### C8 — חיבור ה-DB היה "קוד מת" מטעה ✅ תוקן
ב-Program.cs היה בלוק שקורא ConnectionString מ-appsettings.json ולא עושה איתו כלום (השורה המחברת הושארה בהערה — וממילא לא הייתה יכולה לעבוד: `MyController.DBConn` הוא ReadOnly). בפועל החיבור מגיע **רק** מ-app.config (`TargCCOrders.Controller`). הוסר הקוד המטעה, נוספה ולידציה בעליית האפליקציה שנופלת עם הודעה ברורה אם ההגדרה חסרה.

---

## 2. ממצאים חשובים נוספים שתוקנו ✅

**אבטחה ותשתית API:**
- אין HTTPS redirect / HSTS → נוסף (בפרודקשן).
- אין הגבלת קצב על Login (חשיפה ל-brute force) → Rate limiter: 10 ניסיונות לדקה לכל IP.
- כותרות אבטחה (X-Content-Type-Options, X-Frame-Options, Referrer-Policy) → נוספו.
- Batch PATCH איפשר לשנות **כל** שדה בישות דרך reflection — כולל שדות שמורים לשרת כמו עלות קנייה (UnitCost) — עם בליעת שגיאות שקטה → הוחלף במנגנון allow-list לפי ה-UpdateDto (`ccPatchHelper.cs`) עם דיווח שגיאות מפורש. `Ids` תוקן מ-int ל-long.
- בדיקת ה-ETag (עריכה מקבילה) בלעה חריגות והמשיכה לשמור → עכשיו מחזירה 409.
- Health check "בדק" DB בלי לגשת אליו בכלל (תמיד ירוק) → עכשיו מבצע שאילתה אמיתית.
- דליפת הודעות שגיאה פנימיות ב-Audit → הודעה גנרית + לוג Serilog.

**חוויית משתמש (React):**
- דשבורד הציג **נתונים מומצאים** (גרף הכנסות hardcoded, גרף גיול חובות מפוברק ממקדמים) — מסוכן מאוד מול מנכ"ל → endpoint אמיתי `/api/dashboard/summary` + כל ה-KPIs והגרפים מנתוני אמת.
- מסך ההזמנה הציג מספרי ID במקום שמות מוצרים/לקוחות → תוקן דרך ה-composite endpoint.
- תאריך הזמנה דיפולטי היה ב-UTC — הזמנת ערב קיבלה תאריך של אתמול/שעה שגויה → זמן מקומי.
- עיגול כספים — חישובי float גולמיים יצרו סטיות אגורות מול השרת → עיגול לשתי ספרות בכל נקודות החישוב.
- מספר הזמנה חייב הקלדה ידנית (התחיל 0 עם שגיאת ולידציה) → מולא אוטומטית מהשרת.
- Ctrl+S לשמירה + אזהרת "שינויים לא נשמרו" ביציאה מטופס ההזמנה.
- כפתורים מתים: טאבים במסך הזמנות שלא סיננו כלום (הוסתרו), Print בלי onClick (חובר), Escape שזרק החוצה מטופס באמצע הקלדה (הוסר).
- עברית: MUI עבר ל-heIL (עימוד, טבלאות, דיאלוגים), מסך Login תורגם, פירורי לחם ותפריט תורגמו, חצי "חזור" תוקנו לכיוון RTL, מסכי ה-CRUD הגולמיים באנגלית (orderHeaders/orderLines) הוסתרו מהתפריט.
- ולידציה שגויה בטופס הזמנת ספק (דרשה שנושא המייל יהיה... כתובת מייל) → תוקן.

**פיצ'רים שהיו באקסל וחסרו — נוספו:**
- **שליחת הזמנה לספק (ביובי)** — כמו גיליון "שליחה": `POST /api/supplierOrders/{id}/send` מרכיב מייל HTML עם טבלת המוצרים במחירי קנייה, שולח ב-SMTP אם מוגדר, ואם לא — מחזיר את המייל המוכן להעתקה. נוסף כפתור "שלח לספק" בטופס.
- **מנוע חובות** — כלל האקסל (סכום > ₪100 וגם > 10 ימים) מיושם ב-`sp_UpdateDebtAttention` עם ספים הניתנים לשינוי בהגדרות. גם באג "החוב הנעלם" תוקן (חוב עם PaidAmount=NULL נעלם מדוח החובות).
- **מעקב קוני כוורות** — `sp_RefreshBeehiveTracking` מעדכן תאריך רכישה אחרונה וכמות מתוך ההזמנות בפועל (במקום שדות ידניים שמתיישנים).
- **שדות מהאקסל שחסרו ב-DB:** מספר לקוח בריווחית (עם אינדקס ייחודי), צורת משלוח דיפולטית ללקוח, טלפון רחב יותר.

**סכמת DB (בסקריפט):**
- טבלאות זבל נמחקו (CheckPrg, CheckPrg1, zzPreviousRole — עם גיבוי).
- Sequence למספרי הזמנה (מונע התנגשות בהזנה מקבילה).
- טריגר יצירת המשלוח תוקן (כתובת ארוכה הפילה את כל ההזמנה; כתובת ריקה איפסה גם עיר; נוצרו כפילויות).
- אילוצי שלמות: כמות>0, מחיר≥0, הנחה 0–100, חודש תזכורת 1–12, ייחודיות שורה בהזמנה ורשומת מעקב ללקוח.
- היסטוריית מחירים חוברה סוף-סוף: טריגר ארכוב אוטומטי בשינוי מחיר + FK + אינדקסים (+נמחקה עמודת "AddFieldsHere" שנשכחה מהתבנית).
- הזמנת ספק כבר לא כבולה 1:1 להזמנת לקוח (OrderHeaderID הפך ל-NULL-able) + טריגר סכום מצטבר.
- אינדקסים חסרים: תאריך משלוח, מספר חשבונית, סטטוס חוב.

---

## 3. מה נשאר — כנות מקצועית

**חסם בנייה שחובה לבדוק אצלך (לא יכולתי לבדוק כאן — אין Windows/SQL בסביבה שלי):**
ה-Host (net8) מפנה ל-`DBController.vbproj` בפורמט הישן (non-SDK, .NET Framework 4.8). ברוב התצורות `dotnet build` לא יודע לצרוך הפניה כזו. אם ה-build נכשל אצלך: לבנות את DBController ב-Visual Studio ולהפנות ל-DLL, או (עדיף לטווח ארוך) להסב את DBController לפרויקט SDK-style. בכל מקרה — **המערכת רצה על Windows בלבד** (WMI, EventLog, Drawing).

**דברים שלא בניתי ושווים ספרינט המשך, לפי סדר ערך עסקי:**
1. **ייצוא לריווחית** — אין שום קוד ריווחית בכל הפתרון. באקסל זה היה תהליך שבועי מרכזי. צריך endpoint שמייצר את קובץ הייצוא (יש לנו עכשיו את שדה RivhitCustomerNo כתשתית).
2. **Paging בצד השרת** — כל מסך רשימה טוען את כל הטבלה לזיכרון ואז מסנן/ממיין ב-reflection. ב-7,000 הזמנות זה יחזיק לדמו; ב-20,000+ זה יזחל. הפתרון הנכון: פרוצדורות עם TOP/OFFSET דרך FillByParameters של TargCC.
3. **תזכורות כוורות** — הנתונים מתעדכנים אוטומטית עכשיו, אבל אין התראה יזומה (מייל/מסך "מי להזכיר החודש"). קל להוסיף מסך שמסנן לפי ReminderMonth = החודש הנוכחי.
4. **הזנת שורות במקלדת בקצב אקסל** — Enter לשורה הבאה, הוספת שורה אוטומטית אחרי בחירת מוצר, חיפוש מוצר גם לפי מק"ט, "שמור וחדש". זה ההבדל בין "עובד" ל"מרגיש טוב יותר מהאקסל".
5. **לוקליזציה מלאה של 31 מסכי ה-CRUD המיוצרים** — כרגע הוסתרו העיקריים; אם מריצים TargCC מחדש עדיף לתקן בתבנית הגנרטור, לא ביד.
6. **הסטאק הישן (WinForms/Admin)** — יש בו באגים אמיתיים (Quick-Pay מסמן "שולם" גם בביטול, יצירת חוב כפול בלחיצה כפולה, מספור חשבוניות מזויף בזיכרון). לא נגעתי — ההמלצה שלי: להקפיא אותו ולעבוד רק דרך ה-Web. אם מתכוונים להמשיך להשתמש בו — תגיד לי ואתקן גם שם.
7. **גיט!** הפרויקט לא נמצא בשליטת גרסאות בכלל, ותיקיית dist (3MB) יושבת בתוך הסורס. זה הדבר הראשון שהייתי מסדר.

**הערה על התכנון המקורי (ה-ER):** התכנון בדיאגרמה טוב ביסודו והמימוש די נאמן לו. הסטייה המשמעותית שמצאתי לטובה: פיצול OrderHeader/OrderLine (בדיאגרמה orders היה שטוח יותר). הסטיות לרעה היו בביצוע, לא בתכנון — עמודות clc_ שאינן מחושבות, והיעדר product_prices ייחודי — שתיהן טופלו.

---

## 4. צ'קליסט חובה לפני עלייה לאוויר ⚠️

1. **גיבוי DB** ואז הרצת `Database/DB_MIGRATION_2026-07-16.sql` על TargCCOrdersNew.
2. **מפתח JWT אמיתי** — 32+ תווים אקראיים, במשתנה סביבה `Jwt__AdminKey` (לא בקובץ בקוד).
3. **app.config** — לעדכן `TargCCOrders.Controller` לשרת הפרודקשן, לוודא ש-`C:\Logs\Apps` קיים וכתיב.
4. **בניית הפתרון בVisual Studio** — לוודא שה-Host נבנה מול DBController (ראה חסם הבנייה למעלה) ולבדוק E2E: login → יצירת הזמנה עם שורות → סכומים לא-אפס → מחיקת שורה → דשבורד.
5. **CORS + HTTPS** — להגדיר את הדומיין האמיתי ב-appsettings (`Cors:AllowedOrigins`) ותעודת SSL.
6. **תזמון יומי** (SQL Agent או TaskManager): `EXEC sp_UpdateDebtAttention; EXEC sp_RefreshBeehiveTracking;`
7. אופציונלי לשליחת מייל לספק: להגדיר SMTP ב-app.config (`TargCCOrders.SMTPServer/Port/EmailFrom/UserName/Password`).
8. אחרי המיגרציה: **להריץ TargCC מחדש** (Sprocs + DBController + WebAPI) כדי לחשוף את השדות החדשים של הלקוח (ריווחית, משלוח דיפולטי) ב-UI. השינויים שלי בקונטרולרים נמצאים בקבצים מיוצרים — אם מריצים גנרטור, לשלב את התבנית (בעיקר את דפוס ה-RequesterFactory) בתבניות ה-CCV2, אחרת התיקונים יידרסו.

---

## 5. קבצים שנוצרו/שונו

**חדשים:** `Database/DB_MIGRATION_2026-07-16.sql` · `WebAPI/Controllers/ccRequesterFactory.cs` · `ccPatchHelper.cs` · `ccOrdersCompositeController.cs` · `ccPricingController.cs` · `ccDashboardController.cs` · `ccSettingsController.cs` · `ccSupplierOrderSendController.cs` · ב-React: `SettingsApi/PricingApi/OrderCompositeApi/DashboardApi` + hooks.

**שונו:** `Program.cs` (אבטחה+ולידציית קונפיג) · `ccAuthController.cs` (Ticket+rate limit+הסרת מפתח קשיח) · כל 11 הקונטרולרים (Requester אמיתי, ETag, PatchHelper) · `ccAuditController.cs` · `ccHealthController.cs` · ב-React: `OrderCompositeForm/View/List`, `Dashboard`, `DebtManagementList`, `SupplierOrderForm`, `LoginPage`, `App.tsx`, `routes.ts`, `ThemeContext`, `ConfirmDialog`, `he.ts`, `types/SupplierOrder.ts`.

**אימות שבוצע כאן:** `tsc --noEmit` עובר נקי על ה-React; קונטרולרים חדשים נכתבו מול חתימות ה-VB האמיתיות (נבדקו אחת-אחת); את בניית ה-.NET המלאה חובה להריץ אצלך (Windows).
