# TargCCOrders — מסמך העברה והמשך עבודה

> **הדבק את כל המסמך הזה ל-Claude בתחילת הסשן.**
> נכתב 18.8.2026 · מיועד להמשך העבודה מול האפיון של הלקוח.
> המערכת **בייצור ועובדת**. מה שנשאר הוא התכנסות לאפיון.

---

## 0. קרא את זה קודם — חמש מלכודות שיעלו לך יום עבודה כל אחת

**1. אל תריץ TargCC (CC).** הוא ידרוס את תיקוני המע"מ, ה-enums, 27 הקונטרולרים
ו-16 הפרוצדורות. **CC מת בפרויקט הזה** — כל שינוי סכמה נכתב ידנית מעכשיו.

**2. סחיפת ordinal — הבאג החוזר.** שכבת ה-VB קוראת עמודות **לפי מיקום**. הוספת
עמודה ל-`SELECT` בפרוצדורה בלי לייצר מחדש את ה-VB שוברת הכל, **והבאג רדום כל
עוד העמודה NULL**. קרה שלוש פעמים: `VATRatePercent`, `RivhitCustomerNo`,
`clc_RemainingAmount`.

**3. הוספת ערך enum דורשת שלוש עריכות, לא שתיים.** ראה §3 — זה תועד בדם.

**4. `sqlcmd` תמיד עם `-I`**, ולעברית `-f 65001`. בלי `-I` פרוצדורה שנוצרת
נכשלת בזמן ריצה בלי שום אזהרה ביצירה. עברית בקונסולה מתרסקת ל-`?????` —
לניתוח נתונים בעברית להשתמש ב-Node או לכתוב לקובץ ולקרוא אותו.

**5. הידור נקי אינו הוכחה.** שני הבאגים הגרועים בפרויקט (`System.Management`,
סחיפת ordinal) עברו הידור מושלם ונכשלו בזמן ריצה. **תמיד לבדוק חי מול ה-DB.**

---

## 1. הקשר ונתיבים

מערכת ניהול הזמנות למוצרי הדברה ביולוגית, כוורות ומלכודות. מחליפה גיליון
Google Sheets שנוהל מ-2020.

```
קוד:      C:\Dev\NonTFS\TargCCOrders
Publish:  C:\Dev\Publish\TargCCOrders
שיתוף:    \\mercury\Public\DoronG        ← החבילה מועתקת לשם אוטומטית
DB:       localhost\TargCCOrdersNew
ייצור:    https://orders.target.co.il    (IIS, תעודה תקפה)
git:      https://github.com/dorongut1/TargCCOrders
```

| שכבה | טכנולוגיה |
|---|---|
| `DBController` | VB.NET, .NET Framework 4.8 — **מיוצר ע"י TargCC, נערך ידנית** |
| `WebAPI/Controllers` | C# — מיוצר + קונטרולרים מותאמים בשם `cc*` |
| `TargCCOrders.WebAPIHost` | .NET 8 — מגיש גם API וגם React מ-`wwwroot` |
| `TargCCOrders.ReactUI/ReactUI` | React + TypeScript + MUI + react-query, RTL |
| `TargCCOrdersAdmin` | WinForms — **הוקפא, לא לגעת** |

**MSBuild** (אין `dotnet build` תקין בגלל `.vbproj` בפורמט ישן):
```
C:\Program Files\Microsoft Visual Studio\18\Professional\MSBuild\Current\Bin\MSBuild.exe
```

---

## 2. מקורות הדרישות — שלושה מסמכים, ולא תמיד מסכימים

| מסמך | מה זה | מעמד |
|---|---|---|
| `אפיון_מערכת_הזמנות_v2.docx` (יוני) | אפיון פורמלי, 7 מסכים, 12 סעיפים | בסיס |
| `פגישה עם עופר ואילון 17.8.docx` | סיכום פגישה — מעשי בהרבה | **קובע** במקום שיש סתירה |
| `מערכת תיפעולית -הזמנות 2026-2025.xlsx` | הגיליון החי | **מקור האמת לנתונים** |

**המסמכים המנתחים שכבר נכתבו — קרא אותם:**

| קובץ | תוכן |
|---|---|
| `SETUP_DEV_ENVIRONMENT.md` | **התחל כאן** — הקמת סביבה מקומית צעד אחר צעד |
| `GAP_ANALYSIS_SPEC_v2_2026-08-17.md` | ניתוח פערים מלא מול האפיון |
| `RESPONSE_TO_SPEC_v2_2026-08-17.md` | **מסמך לשליחה ללקוח** — נקי, לא פנימי |
| `BUILD_PLAN_2026-08-17.md` | התוכנית והגלים, טבלת ביצוע מפורטת |
| `SPIKE_1.1_ENUM_EXTENSIBILITY_2026-08-18.md` | ממצאי הספייק — קרא לפני נגיעה ב-enums |
| `DEPLOY_TO_SERVER.md` | פריסה מלאה, כולל מלכודת WebDAV |

### סתירות פתוחות שדורשות הכרעת לקוח

| נושא | האפיון | הפגישה | מצב |
|---|---|---|---|
| **ריווחית** | "אין API — ייצוא אקסל" | "התממשקות ל-API" | ✅ **נפתר** — יש API מלא, ראה §6 |
| **תמחור** | טבלת מחירון אישי | מחיר מההזמנה הקודמת | הוכרע: **פופ-אפ בחירה** |
| **סטטוסים** | 8 בשרשרת אחת | "להוריד את השדה" | הוכרע: **שני צירים** — ראה §5 |

---

## 3. ⚠️ הוספת ערך enum — הנוהל המדויק

**זה תועד אחרי שהבדיקה תפסה מה שקריאת קוד פספסה.** נדרשות **שלוש** עריכות:

```vb
' 1. DBController/CC/Support/csSptEnums.vb — חבר ה-enum
Public Enum enmDeliveryMethod
    ...
    [Elkana]        ' ← בסוף, לא לפי א״ב. שומר על המספרים הקיימים

' 2. אותו קובץ — TranslateEnmDeliveryMethod (מחרוזת → enum)
      Case "elkana"
        pDeliveryMethod = enmDeliveryMethod.Elkana

' 3. אותו קובץ — FastToString (enum → מחרוזת) ← ה-Select Case שכולם שוכחים
      Case clsEnums.enmDeliveryMethod.Elkana : Return "Elkana"
```

**בלי #3 הערך נקרא נכון ונשמר כ-`UD`.** זו בדיוק התסמונת שנתפסה ב-18.8.

**ובנוסף** — שורה ב-`c_Enumeration` לתווית העברית (ראה
`Database\ADD_DeliveryMethods_2026-08-18.sql` כתבנית).

### רשת הביטחון — הרץ אותה אחרי כל נגיעה ב-enum

```bash
sqlcmd -S localhost -d TargCCOrdersNew -E -I -b -h-1 -W -Q "UPDATE OrderHeader SET enmDeliveryMethod='Elkana' WHERE ID=10019;"
curl.exe -s -X POST http://localhost:5199/api/diagnostics/orderRoundTrip/10019
sqlcmd -S localhost -d TargCCOrdersNew -E -I -b -h-1 -W -Q "SELECT enmDeliveryMethod FROM OrderHeader WHERE ID=10019;"
```

חייב לחזור `Elkana`. אם חזר `UD` — שכחת את #3.
הנקודה קיימת רק ב-Development ואינה דורשת התחברות בכוונה.

### למה `c_Enumeration` אינה טבלת פרמטרים מלאה

`/api/enums` בונה את הרשימה מ-**`Enum.GetValues`** על הטיפוס המהודר
(`ccEnumsController.cs:45`). `c_Enumeration` מספקת **רק את התווית**.
בנוסף, ה-DTO מוגדר כטיפוס ה-enum עצמו — כלומר הכבילה רצה עד ה-React.
**לכן הלקוח לא יכול להוסיף ערך בעצמו**, וזו החלטה מודעת (המשתמש אישר:
"כנראה שהוא לא יוסיף בתדירות").

---

## 4. מה כבר עובד בייצור

| יכולת | מצב |
|---|---|
| כל 30 המסכים בעברית מלאה | ✅ |
| שמות במקום מזהי FK בכל המסכים | ✅ |
| ניהול משתמשים: יצירה, עריכה, מחיקה, תפקידים | ✅ |
| שינוי סיסמה עצמאי (כפתור מפתח בסרגל) | ✅ |
| מסך חובות עם ותק 0-30/31-60/61-90/90+ | ✅ |
| מייל לביובי — תצוגה מקדימה + SMTP | ✅ |
| Audit trail מלא | ✅ |
| הזמנות שומרות מחיר בשינוי מחירון | ✅ (מעצם התכנון) |
| פריסה בלחיצה: `Deploy\Build-Package.bat` | ✅ |

**נתונים ב-DB:** 1,312 לקוחות · 100 מוצרים · 199 שורות מחירון · 147 מעקבי כוורות

**שלוש בדיקות הליבה — לא מדלגים עליהן אחרי שום פריסה:**

| בדיקה | מאמת |
|---|---|
| מסך לקוחות → 1,312 | סחיפת ordinal |
| הזמנה חדשה → סטטוס **"חדש"** ולא "מבוטל" | ה-enum |
| שמור, פתח מחדש → **מע"מ אינו אפס** | המע"מ |

---

## 5. הגלים — תוכנית העבודה

מקרא: 🟢 הושלם · 🟡 חלקי · 🔴 חסר · ⛔ חסום בגורם חיצוני

### גל 1 — תשתית הנתונים (בעבודה)

| מזהה | משימה | מצב | תלות | הוכחת סיום |
|---|---|---|---|---|
| **1.1** | Spike עמידות VB לערך חדש | 🟢 | — | תועד ב-`SPIKE_1.1` |
| **1.2** | מסך ניהול פרמטרים + `EnumMetadata` | 🟢 | 1.1 | ✅ אומת חי 18.8 |
| **1.3** | 4 צורות משלוח חסרות + `IsDelivery` | 🟢 | 1.1 | ✅ 7/7 round-trip |
| **1.4** | **צינור ייבוא — לקוחות ומחירון** | 🔴 | — | ספירות DB = גיליון; הרצה שנייה בלי כפילויות |
| **1.5** | **צינור ייבוא — 2,818 הזמנות 2025–26** | 🔴 | 1.3, 1.4 | סכום כולל = גיליון; דגימת 20 ידנית |
| **1.6** | סטטוס כתוצאה ולא כקלט | 🔴 | — | אין דרך להקליד סטטוס ידנית |
| **1.7** | מקור אמת לעלות — `ProductPrice` בלבד | 🟡 | — | מסך מוצרים בלי מחירים |
| **1.8** | אייקון עין בחובות → פירוט + טלפון | 🔴 | — | נבדק מול חוב רב-הזמנות |
| **1.9** | אימות שוטף+X ממועד ההזמנה | 🟡 | — | הזמנה 1.1 + שוטף 30 → פירעון 31.1 |
| **1.10** | אוטומציית היסטוריית מחירים | 🟡 | — | שינוי מחיר → שורת היסטוריה; הזמנות לא זזות |

**הבא בתור: 1.4 → 1.5.** החוסם שלהן הוסר ב-1.3.

### גל 2 — ליבה תפעולית (~3 שבועות)

| מזהה | משימה | תלות |
|---|---|---|
| **2.1** | **אינטליגנציית מסך ההזמנה** — בבחירת לקוח: הזמנה אחרונה + חוב פתוח; בבחירת מוצר: **פופ-אפ בחירה** בין מחיר מחירון למחיר ההזמנה הקודמת, עם חיווי מקור | 1.5 |
| **2.2** | פופ-אפ אוטומטי למייל ביובי אחרי שמירה | — |
| **2.3** | צירוף PDF להזמנה → `D:\Files\Orders\{שנה}\{מס'}` | — |
| **2.4** | מסך ניתוח דמוי Pivot (סוג לקוח × תאריכים) | 1.5 |
| **2.5** | מיפוי קטגוריות למילון הלקוח (9 ערכים) | 1.2 |

### גל 3 — אינטגרציות (חסמים מזוהים)

| משימה | הערכה | מה משחרר |
|---|---|---|
| חשבוניות → ריווחית `Document.New` | 2.5–3.5 שב' | **מפתח API מהחשבון** |
| פאנל דליבר + שליחה אוטומטית לאלקנה/ליאור | 2–3 שב' (API) / 1–1.5 (אקסל) | **מסמך API v1.8** או מבנה אקסל |
| סליקת אשראי → iCredit `GetUrl` | 1.5–2 שב' | פרטי חשבון iCredit |

### גל 4 — עצמאיים

היסטוריה מלאה מ-2020 · WooCommerce · Mobile-first למסך ההזנה ·
MFA בכניסה (התשתית `SetMFA`/`CheckMFA` כבר בנויה) · מודול מלאי

---

## 6. אינטגרציות — מה שנחקר ואומת

### ריווחית — יש API מלא (האפיון טעה)

`https://rivhit-api.readme.io`

| קבוצה | נקודות קצה |
|---|---|
| מסמכים | `Document.New` (חשבונית/תעודת משלוח/הזמנה) · `Document.List` · `Document.Cancel` |
| קבלות | `Receipt.New` · `Receipt.List` |
| לקוחות | `Customer.New/Get/List/Update/Balance/OpenDocuments` |
| **iCredit** | `GetUrl` (דף מתארח) · `SaleChargeToken` · `ChargeSimple` · 3DS · IPN |

**iCredit היא חברת הבת של ריווחית והעסק כבר סולק דרכה באתר** — כלומר
חשבוניות אוטומטיות וסליקת חוב נסגרות דרך ספק אחד עם חוזה קיים.

⚠️ **סליקה: לעולם לא לקלוט מספרי כרטיס בטופס שלנו.** `GetUrl` נותן דף תשלום
מתארח — הכרטיס לא עובר דרכנו ואין חשיפת PCI DSS.

### דליבר — API וגם אקסל

`https://delivers.co.il` · פורטל קליטה: `015.tomika.co.il/imports` · מסמך API v1.8

"אלקנה" ו"ליאור" הם שני **אזורי חלוקה** של אותו מערך שעובד עם דליבר. היום
הפקידה מזינה פעמיים. היעד: הזמנה עם `IsDelivery=1` נשלחת אוטומטית.
**הדגל כבר קיים ב-`EnumMetadata` ומסומן על שני הערכים הנכונים.**

---

## 7. סחיפת הנתונים — הגיליון החי מול ה-DB

| ישות | בגיליון | ב-DB | פער |
|---|---|---|---|
| לקוחות | **2,272** | 1,312 | **960 חסרים** |
| הזמנות 2025–26 | **2,818** (9,360 שורות) | 17 | כמעט הכל |
| מחירון | 184 | 199 | סחיפה דו-כיוונית |

**גיליון "הגדרות" בקובץ החי** מכיל: מע"מ 0.18 · סף חוב ₪100 · ימי חוב 5 ·
וארבע רשימות ערכים. סיווגי הלקוח **תואמים אחד-לאחד**. סיווגי המוצר שלו הם
9 ערכים מול 12 אצלנו — **זו התשובה לשאלת "מילון הקטגוריות" מהאפיון**.

**כלי הייבוא:** `Database\tools\` (README בפנים) + `IMPORT_1_staging_2026-08-17.sql`
כתבנית. הצינור צריך להיות **idempotent** — הוא גם החזרה הגנרלית של נדידת הגו-לייב.

⚠️ **מידע אישי לא לגיט:** `Database/import/*.csv` ו-`IMPORT_2_migrate_*.sql`
מוחרגים ב-`.gitignore`. **לעולם לא `git add -A`** — להוסיף קבצים בשמם המפורש.

---

## 8. מחזור הפיתוח והפריסה

### בנייה ואימות מקומי

```bash
cd C:\Dev\NonTFS\TargCCOrders\TargCCOrders.ReactUI\ReactUI
npx tsc --noEmit          # חייב להיות נקי
npx vitest run            # 20 בדיקות
```

```bash
cmd /c "npm run build & echo EXIT=%ERRORLEVEL%"
```
> `npm run build` מחזיר exit 1 ב-PowerShell גם כשהוא מצליח. זו הדרך לאמת באמת.

```bash
& "C:\Program Files\Microsoft Visual Studio\18\Professional\MSBuild\Current\Bin\MSBuild.exe" "TargCCOrders.WebAPIHost\TargCCOrders.WebAPIHost.csproj" -t:Build -v:m -p:Configuration=Debug
```

### הרצה חיה מקומית

```bash
cd TargCCOrders.WebAPIHost\bin\Debug\net8.0
$env:ASPNETCORE_ENVIRONMENT='Development'; .\TargCCOrders.WebAPIHost.exe --urls http://localhost:5199
```
לפני זה — להעתיק את `dist` ל-`bin\Debug\net8.0\wwwroot`, אחרת יוגש ממשק ישן.

### פריסה — לחיצה אחת

```
Deploy\Build-Package.bat
```
בונה, אורז, **מוחק את שלושת קבצי ה-config מהחבילה**, ומעתיק ל-`\\mercury\Public\DoronG`.
אם ההעתקה נכשלת הסקריפט **נעצר** — כדי שלא תפרוס ארכיון ישן בטעות.

**בשרת, PowerShell מוגבה:**

```bash
Stop-WebAppPool -Name 'Orders.target.co.il'; $n=0; while ((Get-WebAppPoolState -Name 'Orders.target.co.il').Value -ne 'Stopped' -and $n -lt 40) { Start-Sleep -Milliseconds 500; $n++ }; "state=" + (Get-WebAppPoolState -Name 'Orders.target.co.il').Value
```

⚠️ **IIS מקים את הפול מחדש ברגע שמגיעה בקשה.** לוודא `state=Stopped` ולפרוס מיד.

```bash
Expand-Archive -Path "\\mercury\Public\DoronG\<קובץ>.zip" -DestinationPath "D:\Webs\Orders.target.co.il" -Force
```

```bash
Start-WebAppPool -Name 'Orders.target.co.il'
```

`web.config` ו-`TargCCOrders.WebAPIHost.dll.config` **אינם בארכיון** — מחרוזת
החיבור, `LogLocation` ותיקון ה-WebDAV שורדים פריסות.

⚠️ **`Expand-Archive` ולא גרירה ב-Explorer** — הוא דורס קבצים בודדים ולא נתקע
על handle של תיקייה.

### מלכודת WebDAV — אם `PUT`/`DELETE` מחזירים 405

IIS מגיע עם `WebDAVModule` מופעל והוא תופס את שתי הפעולות **לפני** ASP.NET Core.
מקומית זה עובד (Kestrel), ולכן **התקלה מתגלה רק אחרי פריסה ונראית כמו באג בקוד**.
התיקון קיים ב-`web.config` בשרת ושורד פריסות. **הבדיקה המבחינה:** `PUT` שמחזיר
**401** הגיע לאפליקציה; **405** נחסם ב-IIS.

---

## 9. מצב הענף `feature/parameter-management`

**5 commits, לא נדחף.** בסיס: `1e29cfb` על `main`.

| Commit | תוכן |
|---|---|
| `57b5013` | Spike: `c_Enumeration` היא טבלת תוויות, לא ערכים |
| `26f0d0f` | הוכחת אובדן הנתונים + נקודת אבחון `orderRoundTrip` |
| `23624c5` | תיקון: הכבילה ל-enum רצה עד ה-React |
| `2d209a2` | 4 צורות המשלוח + התוויות |
| `44a3aea` | מסך ניהול פרמטרים |

**סקריפטי DB שיש להריץ בכל סביבה:**
```
Database\ADD_DeliveryMethods_2026-08-18.sql
Database\CREATE_EnumMetadata_2026-08-18.sql
```
שניהם idempotent ונבדקו בהרצה כפולה.

### מה אומת חי (18.8)

| בדיקה | תוצאה |
|---|---|
| 18 צורות משלוח במסך | ✅ |
| 4 החדשות עם תוויות עבריות | ✅ |
| דגל דליבר על `Elkana` + `LiorCarmiel` בלבד | ✅ |
| ספירת שימוש אמיתית מה-DB | ✅ |
| עריכת תווית → מתעדכנת ב-`/api/enums` | ✅ |
| **מסך הזמנה מרנדר את התווית החדשה** | ✅ `שיטת משלוח: אלקנה (מרכז)` |
| רשימה לא מנוהלת נדחית | ✅ 400 |
| תווית ריקה נדחית | ✅ 400 |
| round-trip 7/7 כולל ערכים קיימים | ✅ |

---

## 10. מה צריך מהלקוח (הכל זול עבורו)

1. **מפתח API של ריווחית** — משחרר את כל מודול החשבוניות
2. **מסמך API v1.8 של דליבר**, או מבנה האקסל שהם קולטים
3. **פרטי חשבון iCredit** + אישור לסליקה מתוך המערכת
4. **אישור מיפוי הקטגוריות** — 9 שלו מול 12 שלנו
5. **חצי שעה של בר עם המערכת בטלפון** — קריטריון גו-לייב מפורש באפיון,
   **עדיין לא בוצע**

---

## 11. חובות פתוחים שאסור לשכוח

| נושא | חומרה |
|---|---|
| **סיסמת `1234` קבועה בקוד** לכל משתמש חדש (`ccUserAdminController.cs:46`) — על כתובת ציבורית | 🔴 גבוהה |
| `ProductPriceHist.enmCustomerType` — הטיפוס `number`, ה-DB `nvarchar`. רדום (0 שורות) | 🟡 |
| 16 מוצרים בלי מחיר — ההתראה עובדת, הנתונים חסרים | 🟡 |
| כפילות תוויות: `Credit` ו-`CreditCard` שניהם "אשראי" | 🟡 |
| `Administrator` אינו ברשימת מנהלי המשתמשים (`Master`/`ApplicationMaster`/`UserManager`) | 🟡 |

---

## 12. איך לעבוד כאן

- **קרא את §0 לפני שאתה נוגע בקוד.** רוב הבאגים כאן חזרו על עצמם.
- **הידור נקי אינו הוכחה** — תמיד לבדוק חי מול ה-DB.
- **פקודה אחת בכל פעם** בעבודה מול השרת.
- **כל סקריפט DB נשמר ב-`Database\`** — תיקוני 9.8 הוחלו ישירות ונעלמו,
  וזה עלה חודש.
- **תגיד את האמת המקצועית** גם כשהיא לא נוחה, ואם משהו לא אומת — לומר זאת
  במפורש. מסמכי הפרויקט מבחינים בכוונה בין "אומת" ל"מסקנה".
